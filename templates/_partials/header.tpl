<header id="header">
  <div class="container">
    <a href="{$urls.base_url}">
      <img src="{$shop.logo}" alt="{$shop.name}" class="logo">
    </a>
    {hook h='displayTop'}
    {hook h='displayNav'}
    {hook h='displaySearch'}
  </div>
</header>
